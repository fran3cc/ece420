## rPPG Live Demo

- Activate venv: `final_project\.venv\Scripts\Activate.ps1`
- Install deps: `pip install -r final_project\requirements.txt`
- Run: `python final_project\demos\demo_rppg_live.py --camera 0`

Notes: `q` quits, `s` saves. Use another `--camera` index if needed.